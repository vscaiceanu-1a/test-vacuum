extends:
  - recommended

plugins:
  - '@ama-openapi/redocly-plugin'
  - './dist/index.js'

rules:
  custom-rules/no-properties-with-schema-composition: error

apis:
  shop@v1:
    root: apis/OnlineShopApi.v1.yaml
    output: bundle/specification.yaml
